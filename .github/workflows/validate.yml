name: Validate data

on:
  pull_request:
    paths:
      - "data/**"
      - "assets/previews/**"
      - "scripts/validate-data.py"
      - "scripts/validate-partials.py"
      - "partials/**"
      - "js/partials.js"
      - "i18n/**"
      - "scripts/build-i18n.py"
      - "scripts/i18n_html.py"
      - "scripts/i18n_catalog.py"
      - "scripts/validate-i18n.py"
      - "*.html"
      - ".github/workflows/validate.yml"

jobs:
  validate:
    name: Validate design JSON
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Validate JSON and preview files
        run: python3 scripts/validate-data.py

      - name: Validate shared chrome partials
        run: python3 scripts/validate-partials.py

      - name: Validate translations and generated locales
        run: python3 scripts/validate-i18n.py
